读取文件内容，带行号显示。

参数：
- path: 文件路径（必需）
- offset: 起始行号，从 0 开始（可选）
- limit: 读取行数（可选，默认全部）

输出格式：
每行前缀行号（1-indexed），如：
    1  use std::io;
    2  fn main() {

适用场景：
- 查看源代码
- 检查配置文件
- 分析日志文件

大文件处理：
- 使用 offset + limit 分段读取
- 输出末尾会提示剩余行数