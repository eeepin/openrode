执行 shell 命令，返回 stdout + stderr。

使用规则：
- 命令在 bash 中执行
- 返回命令的标准输出和标准错误
- 非零退出码会在输出前加上 "exit <code>"
- 输出超过 50KB 会被截断（保头保尾）

适用场景：
- 运行测试、构建项目
- 查看进程、网络状态
- Git 操作
- 安装依赖

注意：
- 长时间运行的命令会阻塞
- 危险命令（rm -rf /）请谨慎使用